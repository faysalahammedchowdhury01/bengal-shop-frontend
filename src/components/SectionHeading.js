import React from 'react';
import classes from '../styles/SectionHeading.module.css';

const SectionHeading = ({ children }) => <div className={classes.sectionHeading}>{children}</div>;

export default SectionHeading;
